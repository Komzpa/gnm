1) Запускать экзешник по адресу:
app\model_test-build-desktop-Qt_4_8_3_STATIC_______\release

2) Созданные шейпфайлы пока отправляются в temp\

3) Не забыть, что в каталоге с экзешником тестового приложения должны лежать необходимые dll OGR. Я кидал всё, что в \<корень GDAL>\bin

4) Ещё настройки для использования OGR dll:

* Изменил Makefile.Debug в каталоге сборки:

	- к INCPATH добавил -I"c:\OSGeo4W\include"
	- к LIBS добавил c:\OSGeo4W\lib\gdal_i.lib

* При добавлении любых файлов в проект Qt, Makefile.Debug перестраивается, поэтому добавлять эти строки надо заного.
TODO: разобраться, и добавлять пути в файл проекта